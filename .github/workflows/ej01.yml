name: exercise 01
on: push
jobs:
  setup-node:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/cache@v4
        with:
          key: npm-cache
          path: ~/.npm
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - run: npm ci --verbose
